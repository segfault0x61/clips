import { FbTimestrampPipe } from './fb-timestramp.pipe';

describe('FbTimestrampPipe', () => {
  it('create an instance', () => {
    const pipe = new FbTimestrampPipe();
    expect(pipe).toBeTruthy();
  });
});
